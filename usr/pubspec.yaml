name: nzaromill_manager
version: 1.0.0+1
description: NzaroMill Manager - Machine management and spare parts inventory system

publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  image_picker: ^1.1.2
  intl: ^0.19.0
  syncfusion_flutter_charts: ^26.1.39
  # TODO: Add Firebase dependencies when backend is connected
  # firebase_core: ^3.0.0
  # firebase_auth: ^5.0.0
  # cloud_firestore: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/

  # TODO: Add camera/gallery permissions in android manifest when needed
